# 订单模板功能使用指南

## 功能概述

订单模板功能允许你为常用的客户和订单类型创建模板，从而快速创建新订单，无需重复输入相同的信息。

## 主要功能

### 1. 创建模板

1. 从主页点击 **"Booking Templates"** 菜单
2. 点击 **"New Template"** 按钮
3. 填写模板信息：
   - **模板名称**（必填）：如 "新加坡航空 - 常规"
   - **描述**（可选）：简短说明模板用途
   - **客户信息**：客户名称、电话、邮箱、地址等
   - **Tour信息**：Tour Code、Tour Description
   - **Items**：添加固定的服务项目和价格
   - **Passengers**：添加常用的乘客信息
4. 点击 **"Save Template"** 保存

### 2. 从模板创建订单

有两种方式：

#### 方式一：从模板列表
1. 进入 **Booking Templates** 页面
2. 找到要使用的模板
3. 点击 **"Create Order"** 按钮
4. 系统会自动填充模板中的所有信息
5. 填写**航班日期和时间**等变动信息
6. 保存订单

#### 方式二：从新建订单页面
1. 手动在浏览器地址栏添加 `?templateId=X`（X为模板ID）
2. 系统会自动加载模板数据

### 3. 编辑模板

1. 在模板列表页面找到要编辑的模板
2. 点击**编辑图标**（铅笔图标）
3. 修改模板信息
4. 点击 **"Save Changes"** 保存

### 4. 删除模板

1. 在模板列表页面找到要删除的模板
2. 点击**删除图标**（垃圾桶图标）
3. 确认删除操作

## 使用场景

### 场景1：固定商家，固定路线
- 创建模板：客户A + 新加坡航空 + 常规乘客
- 每次开单：只需更改出发日期和航班号
- 节省时间：90%的信息自动填充

### 场景2：旅行团模板
- 创建模板：旅行社 + 固定的团员名单
- 每次开团：只需更改日期即可
- 批量处理：多人订单快速生成

### 场景3：企业客户
- 创建模板：企业客户 + 常见商务舱配置
- 每次开单：选择模板 + 更改日期
- 标准化：确保每次开单格式一致

## 模板包含的信息

### ✅ 会被保存到模板的信息
- 客户名称、电话、邮箱、地址
- Discount、Staff
- Tour Code、Tour Description
- Special Notes
- 所有Items（项目名称、数量、单价）
- 所有Passengers（姓名、护照、生日、护照到期日）

### ❌ 不会保存到模板的信息（需要每次手动填写）
- Booking Date（开单日期）
- 航班信息（出发/到达日期、时间、航班号、目的地）
- 这些信息通常每次都不同，所以需要在创建订单时手动填写

## 注意事项

1. **模板名称必须唯一**：不能创建同名模板
2. **必填字段**：
   - 模板名称
   - 客户名称
   - 客户电话
   - 至少一个Item
3. **从模板创建订单后**：
   - 记得填写航班日期和时间
   - 可以修改任何自动填充的信息
   - 保存订单不会影响原模板

## 技术细节

### 数据库表
- `booking_templates`：模板主表
- `booking_template_items`：模板项目表
- `booking_template_passengers`：模板乘客表

### API路由
- `GET /api/booking-templates`：获取所有模板
- `POST /api/booking-templates`：创建新模板
- `GET /api/booking-templates/[id]`：获取单个模板
- `PUT /api/booking-templates/[id]`：更新模板
- `DELETE /api/booking-templates/[id]`：删除模板
- `POST /api/booking-templates/[id]/create-order`：从模板创建订单

### 页面路由
- `/booking-templates`：模板列表
- `/booking-templates/new`：创建新模板
- `/booking-templates/[id]/edit`：编辑模板
- `/booking-orders/new?templateId=X`：从模板创建订单

## 常见问题

**Q: 修改模板会影响已创建的订单吗？**
A: 不会。从模板创建订单后，订单和模板是独立的。修改模板只影响以后创建的订单。

**Q: 可以从现有订单创建模板吗？**
A: 目前暂不支持。需要手动创建模板。未来可以考虑添加"保存为模板"功能。

**Q: 模板中的客户如果被删除了怎么办？**
A: 模板存储的是客户名称的副本，不会因为客户表变化而受影响。

**Q: 可以复制模板吗？**
A: 目前暂不支持直接复制。可以通过编辑模板，修改名称来创建相似模板。

## 下一步优化建议

1. **从现有订单创建模板**：在订单详情页添加"保存为模板"按钮
2. **模板复制功能**：允许快速复制现有模板
3. **模板分类/标签**：方便管理大量模板
4. **模板使用统计**：显示每个模板被使用的次数
5. **批量创建订单**：基于一个模板创建多个订单

---

如有问题或建议，请联系开发团队。
